# Documentation

- `plans/`: reproduction and experiment planning notes.
- `references/`: local paper copies and other large references; PDFs are ignored by Git.
- `phase1_depth_loss_plan.md`: fair-comparison plan for replacing `psi_obj` with `psi_depth`.

