# Generated Assets

This directory is for generated assets that are reused across experiments.

`assets/depthpro/` is ignored by Git because it contains derived depth maps and edge maps. The current StitchBench General DepthPro asset cache is expected at:

```text
assets/depthpro/stitchbench_general/
```

